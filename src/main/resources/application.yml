spring:
  redis:
    database: 0
    host:  127.0.0.1
    port: 6379
    password:
    jedis.pool.max-active: 8
    jedis.pool.max-wait: -1
    jedis.pool.max-idle: 8
    jedis.pool.min-idle: 0
    timeout: 5000